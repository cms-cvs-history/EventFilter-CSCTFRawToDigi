process analyze = {
     untracked PSet maxEvents = {untracked int32 input = 10}

     service = MessageLogger {
       untracked vstring destinations = { "cout"  }
       untracked PSet cout = { untracked string threshold = "INFO" }
       untracked vstring debugModules   = { "*" }
     }

#     source = NewEventStreamFileReader {
#        untracked vstring fileNames = {'file:/tmp/kkotov/GlobalAug07.00017166.0001.A.storageManager.0.0000.dat'}
#        int32 max_event_size = 2000000
#        int32 max_queue_depth = 5
#     }

     source = DaqSource{
       untracked string readerPluginName = "CSCFileReader"
       untracked PSet readerPset = {
         untracked vstring RUI04 = {
           '/tmp/kkotov/csc_00016713_EmuRUI02_Monitor_000.raw'
         }
         untracked vstring FED760 = {'RUI04'}
         untracked int32 firstEvent = 0
       }
     }


     include "EventFilter/CSCTFRawToDigi/data/csctfunpacker.cfi"

     module csctfanalyzer = CSCTFAnalyzer {
         untracked InputTag lctProducer   = csctfunpacker:MuonCSCTFCorrelatedLCTDigi
         untracked InputTag trackProducer = csctfunpacker:MuonL1CSCTrackCollection
     }

     path p = {csctfunpacker,csctfanalyzer}
}
