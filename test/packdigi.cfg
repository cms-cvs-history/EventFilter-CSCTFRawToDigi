process TTT = {
     untracked PSet maxEvents = {untracked int32 input = 10}

     source = PoolSource{
         untracked vstring fileNames = {"file:./digis.root"}
     }

    # packer code
    include "EventFilter/CSCTFRawToDigi/data/csctfpacker.cfi"
    replace csctfpacker.outputFile = "output.raw"

    path p = {csctfpacker}
}
