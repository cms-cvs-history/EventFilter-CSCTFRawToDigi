process TTT = {
     source = PoolSource{
         untracked int32   maxEvents = 10
         untracked vstring fileNames = {"file:digis.root"}
     }

     module csctfpacker = CSCTFPacker {
         untracked bool   zeroSuppression = true
         untracked int32  nTBINs          = 7
         untracked int32  activeSectors   = 64
         untracked string outputFile      = "output.raw"
         untracked bool   putBufferToEvent= true
     }

#    module csctfunpacker = CSCTFUnpacker{ untracked bool TestBeamData = false
#                                          untracked int32 TBFedId  = 4
#                                          untracked int32 TBEndcap = 0
#                                          untracked int32 TBSector = 0
#                                          untracked string MappingFile = "src/EventFilter/CSCTFRawToDigi/test/testmapping.map" }
#     path p = {csctfpacker,csctfunpacker}

     path p = {csctfpacker}
}
